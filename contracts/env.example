# Copy to .env and fill with your own values (never commit .env)
# PRIVATE_KEY must be a hex string prefixed with 0x
PRIVATE_KEY=

# RPC endpoint for Sepolia (public default can be used if not set)
SEPOLIA_RPC_URL=

# Optional: Etherscan API key for verification
ETHERSCAN_API_KEY=



